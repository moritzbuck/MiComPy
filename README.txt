Prereqs python:

* biopython
* pandas
* tqdm
* scipy
* matplotlib

binaries in path:

* makeblastdb
* blastn
* hmmersearch
* prokka

Notes : 

It is built around linux so some stuff brutally uses *nix commands (e.g. find, sed, ...)
